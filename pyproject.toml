[project]
name = "simulador-copa-2026"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "uvicorn>=0.34",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
